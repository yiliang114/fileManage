# See https://help.github.com/ignore-files/ for more about ignoring files.

# dependencies
/node_modules

# testing
/coverage

# production
# jsmap 一般会比较大，所以其实一般都不会上传build目录，但是这个项目特殊之处在于electron需要build之后目录，遂上传
# /build

# misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*

output
